// const [first, second, ...other] = daiktai;

const person = {
	name: "Petras",
	age: 99,
	isMarried: true,
};

const { age, name, isMarried } = person;

console.log(name);
console.log(age);
console.log(personOtherInfo);
